{
  "name": "@shingo/auth-api-client",
  "version": "1.0.0-alpha.6",
  "description": "Client for Shingo auth api",
  "main": "index.js",
  "types": "index.d.ts",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/shingoinstitute/shingo-auth-api.git"
  },
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@grpc/proto-loader": "^0.3.0",
    "@shingo/auth-api-shared": "^1.0.0-alpha.6",
    "grpc": "^1.15.0"
  },
  "devDependencies": {
    "@types/node": "^10.5.4",
    "npm-run-all": "^4.1.3",
    "trash-cli": "^1.4.0",
    "typescript": "^3.0.3"
  },
  "scripts": {
    "build": "run-s clean && run-p build:*",
    "build:proto": "mkdir -p build/proto && cp -R ../../proto build/",
    "build:main": "tsc -b .",
    "clean": "trash build test docs",
    "prepack": "npm run build && cp -Rav build/* . | ../../scripts/parseEmitted.sh build.emit",
    "postpack": "../../scripts/postpack.sh"
  },
  "author": "Abraham White",
  "license": "MIT"
}
